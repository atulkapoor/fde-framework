## What this changes

<!-- One paragraph. If it changes a decision the corpus makes, name the engagement shape that motivated it. -->

## The receipts

- [ ] Tests pinned (a corpus change carries the sentence or profile that motivated it)
- [ ] `pytest -q` green, `ruff check src tests` clean
- [ ] No client material, no credentials, no personal data (CI enforces; check first)
- [ ] If an approach changed: `applies_when`/`avoid_when` still declare real exclusions and cite a case
