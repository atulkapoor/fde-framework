**What this changes and why**

**Checklist**
- [ ] `pytest -q` and `ruff check src tests` pass locally
- [ ] `fde kb validate --root framework` passes if `framework/` changed
- [ ] **No client material** — in code, fixtures, comments, or this PR's
      text. Patterns re-expressed in your own words only.
- [ ] New dimensions carry `weight:`; new approaches carry evidence or a
      note that `fde kb gaps` will report them
- [ ] Behavior changes carry a test that fails without the change
